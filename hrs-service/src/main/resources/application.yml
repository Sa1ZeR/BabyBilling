server:
  port: 3003

spring:
  application:
    name: hrs-service

eureka:
  client:
    service-url:
      defaultZone: 'http://localhost:3000/eureka'
  instance:
    instance-id: ${spring.application.name}:${random.value}
